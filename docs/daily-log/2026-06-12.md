# 2026-06-12

## 今日の作業時間

- 1時間

## 今日やったこと

- ErrorInsightのプロジェクト詳細ページ作成を進めた。
  - 開発中作品として載せていたErrorInsightについて、完成に近づいた状態に合わせて詳細ページの内容を見直した。
  - マニュアル管理システムの詳細ページ構成を参考に、ErrorInsight側も同じ粒度で見せられるように整理した。

- ErrorInsight詳細ページに載せる構成と文章を整理した。
  - プロジェクト概要
  - 解決したい課題
  - 設計ポイント
  - 画面イメージ
  - 動作イメージ
  - 学んだこと
  - リンク
  の流れで見せる方針にした。

- 「解決したい課題」の文章を、画像カード化しやすい形へ整えた。
  - 長文説明ではなく、カード見出しと短い箇条書きで伝える構成にした。
  - 初学者がエラーログでつまずきやすい点を、以下のように分類した。
    - ログの読み方が分からない
    - 重要なエラー箇所を見つけにくい
    - エラーの意味を理解しにくい
    - 調べても理解が残りにくい

- ErrorInsight用の画像ファイル名を決めた。
  - `errorinsight-overview.png`
  - `errorinsight-problems.png`
  - `errorinsight-design-points.png`
  - `errorinsight-screens.png`
  - `errorinsight-learnings.png`
  - 動作動画を追加する場合は `errorinsight-demo.mp4` にする方針にした。

- 動作イメージの載せ方を検討した。
  - GIFではなく、短いMP4動画を `<video>` で埋め込む方針がよいと判断した。
  - 無音・短尺・手動再生を基本にし、Works詳細ページ内で重くなりすぎない見せ方にする。

## 迷ったこと・苦労したこと

- 初学者向けの支援ツールであることを伝えつつ、単なるエラー自動解決ツールに見えないように言葉を調整した。
- 詳細ページ内の説明をHTMLで作る部分と、画像カードとして見せる部分の切り分けを考えた。

## 判断したこと

- ErrorInsightの詳細ページは、マニュアル管理システムと同じく、概要カードと画像カードを組み合わせて見せる。
- ErrorInsightは、エラーを完全に自動解決するツールではなく、調査の出発点を分かりやすく示す補助ツールとして説明する。
- 動作イメージは、見たい人が再生できる短いMP4動画として載せる。
- 画像ファイル名は、`errorinsight-内容.png` の形式で統一する。

## 学んだこと

- 動作イメージをWebページに載せる場合、GIFよりMP4の方がファイルサイズや操作性の面で扱いやすい。

## スクリーンショット

- なし

## 録画

- なし

## 次にやること

- ErrorInsight詳細ページへ画像カードを配置する。
- ErrorInsightのGitHubリンクや資料リンクを整理する。
- 動作イメージ用の短いMP4動画を用意する。
- 待避モーションの不具合を確認する。
  - 配置モーションが二重に流れている可能性があるため、class付与タイミングとアニメーション終了時の処理を調査する。

## 配布ZIP起動テスト結果

- `.venv` 作成後の起動: OK
- `.venv` なし状態での `.bat` 起動: OK
- 展開先をデスクトップへ変更して起動: OK
- 日本語表示の文字化け: なし
- 起動時エラー: なし

配布ZIPを展開した場所に依存せず、`.bat` から正常に起動できることを確認した。

この確認により、仮想環境あり・仮想環境なしの両方で起動できること、また展開先を変更しても実行できることが分かった。録画用のデモでは、インストール手順すべてではなく、`.bat` 起動から解析結果表示までを短く見せる方針にする。
